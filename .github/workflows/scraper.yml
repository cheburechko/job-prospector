name: Scraper Tests
on:
  push:
    branches:
      - main
    paths:
      - 'scraper/**'
      - '.github/workflows/scraper.yml'
  pull_request:
    paths:
      - 'scraper/**'
      - '.github/workflows/scraper.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scraper/src
    steps:
      - uses: actions/checkout@v4
      - name: Run Ruff check
        uses: astral-sh/ruff-action@v3
      - name: Run Ruff format
        uses: astral-sh/ruff-action@v3
        with:
          args: format --diff

  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scraper
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      - name: Install Python dependencies
        run: pip install -r requirements.txt
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('scraper/requirements.txt') }}
      - name: Install Playwright dependencies
        run: python -m playwright install --with-deps chromium
      - name: Run unit tests
        run: pytest src/test/unit -v
      - name: Run integration tests
        run: pytest src/test/integration -v -m integration

  push-image:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Compute short SHA
        id: vars
        run: echo "sha=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::894608133151:role/scraper-gha-ecr-push"
          aws-region: eu-central-1
      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: scraper
          file: scraper/docker/Dockerfile
          push: true
          tags: ${{ steps.ecr.outputs.registry }}/job-prospector/scraper:${{ steps.vars.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
